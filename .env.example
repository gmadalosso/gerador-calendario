# Variáveis de ambiente
# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o arquivo .env no repositório

# Ambiente da aplicação
# Valores possíveis: "production" ou "development"
ENVIRONMENT=production

# Origens permitidas para CORS (Cross-Origin Resource Sharing)
# Deixe vazio para não permitir CORS externo (padrão em produção)
# Para permitir múltiplas origens, separe por vírgula: "https://exemplo1.com,https://exemplo2.com"
# Exemplo: ALLOWED_ORIGINS=https://meu-dominio.com,https://outro-dominio.com
ALLOWED_ORIGINS=
